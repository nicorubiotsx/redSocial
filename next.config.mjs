/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['www.google.com','gcdn.emol.cl'], // Añade el dominio permitido aquí
      },
};

export default nextConfig;
